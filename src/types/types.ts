export interface IFaq  {
    id: number;
    question: string;
    answer: string
}

export interface IRating{
    id: number;
    name: string;
    loaction: string;
    testimonial: string;
    image: string;
}